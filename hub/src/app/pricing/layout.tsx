import { pageMetadata } from '@/lib/siteMetadata'

export const metadata = pageMetadata({ title: 'SlipSurge Memberships and Pricing', description: 'Compare SlipSurge Free, Basic, Advanced, and Ultimate research memberships powered by Whop.', path: '/pricing' })

export default function PricingLayout({ children }: { children: React.ReactNode }) { return children }
