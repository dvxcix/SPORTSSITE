diff --git a/node_modules/uuid/dist/esm-browser/v35.js b/node_modules/uuid/dist/esm-browser/v35.js
index ceb967b..010e9d7 100644
--- a/node_modules/uuid/dist/esm-browser/v35.js
+++ b/node_modules/uuid/dist/esm-browser/v35.js
@@ -26,6 +26,9 @@ export var URL = '6ba7b811-9dad-11d1-80b4-00c04fd430c8';
 export default function (name, version, hashfunc) {
   var generateUUID = function generateUUID(value, namespace, buf, offset) {
     var off = buf && offset || 0;
+    if (buf && (!Number.isSafeInteger(off) || off < 0 || off + 16 > buf.length)) {
+      throw new RangeError('UUID buffer is too small');
+    }
     if (typeof value == 'string') value = stringToBytes(value);
     if (typeof namespace == 'string') namespace = uuidToBytes(namespace);
     if (!Array.isArray(value)) throw TypeError('value must be an array of bytes');
diff --git a/node_modules/uuid/dist/esm-node/v35.js b/node_modules/uuid/dist/esm-node/v35.js
index b1ff623..e7f5249 100644
--- a/node_modules/uuid/dist/esm-node/v35.js
+++ b/node_modules/uuid/dist/esm-node/v35.js
@@ -26,6 +26,9 @@ export const URL = '6ba7b811-9dad-11d1-80b4-00c04fd430c8';
 export default function (name, version, hashfunc) {
   var generateUUID = function (value, namespace, buf, offset) {
     var off = buf && offset || 0;
+    if (buf && (!Number.isSafeInteger(off) || off < 0 || off + 16 > buf.length)) {
+      throw new RangeError('UUID buffer is too small');
+    }
     if (typeof value == 'string') value = stringToBytes(value);
     if (typeof namespace == 'string') namespace = uuidToBytes(namespace);
     if (!Array.isArray(value)) throw TypeError('value must be an array of bytes');
diff --git a/node_modules/uuid/dist/v35.js b/node_modules/uuid/dist/v35.js
index 3aecfa0..c951fe1 100644
--- a/node_modules/uuid/dist/v35.js
+++ b/node_modules/uuid/dist/v35.js
@@ -39,6 +39,9 @@ exports.URL = URL;
 function _default(name, version, hashfunc) {
   var generateUUID = function (value, namespace, buf, offset) {
     var off = buf && offset || 0;
+    if (buf && (!Number.isSafeInteger(off) || off < 0 || off + 16 > buf.length)) {
+      throw new RangeError('UUID buffer is too small');
+    }
     if (typeof value == 'string') value = stringToBytes(value);
     if (typeof namespace == 'string') namespace = uuidToBytes(namespace);
     if (!Array.isArray(value)) throw TypeError('value must be an array of bytes');
